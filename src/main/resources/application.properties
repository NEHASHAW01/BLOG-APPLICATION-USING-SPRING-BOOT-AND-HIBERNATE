spring.datasource.url = jdbc:mysql://localhost:3306/myblog5?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = neha#123SHAW

#hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#ibernate ddl auto
spring.jpa.hibernate.ddl-auto = update

app.jwt-secret= JWTSecretKey 
app.jwt-expiration-milliseconds = 604800000

# JWT token should not be generated by all. only those has key("WTSecretKey") can generate or verify the token.
# session time for the token. - once generating token done, how much time that token can be active.

